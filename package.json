{
"jest": {
    "testMatch": [
        "**/__tests__/**/*.test.js", // Padrão
        "**/__tests__/**/*.feature" // <--- Adiciona suporte a arquivos Gherkin
    ]
},
"scripts": {
    "start": "node server.js",
    "test": "jest --coverage --forceExit" 
    // --forceExit para Jest e Puppeteer trabalharem bem
},

  "devDependencies": {
    "jest": "^30.2.0",
    "jest-cucumber": "^4.5.0",
    "puppeteer": "^24.25.0",
    "supertest": "^7.1.4"
  }
}
